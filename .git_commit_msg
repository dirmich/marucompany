fix: llama.cpp 역시 OpenAI v1/models 규격으로 단일 대통합 및 리팩토링

- 최신 llama.cpp 서버(llama-server)의 OpenAI 호환 규격을 반영하여 /v1/models 엔드포인트 단일 대통합 (routes/settings.ts, ConnectionsPanel.tsx, CooperativeChat.tsx)
- 불필요하고 복잡한 /props 독자 파싱 로직을 걷어내고, OpenAI 표준 data.data.map(m.id) 형태로 LM Studio, vLLM, llama.cpp 파싱 완벽 일원화
- 전체 UI 폰트 크기 대폭 상향 리팩토링 검증 및 tsc 타입 컴파일 / Vite 빌드 완료
