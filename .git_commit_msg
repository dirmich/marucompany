fix: 외부 LLM 모델 목록 파싱 호환성 보완 및 드롭다운 빈칸 렌더링 예방 정합성 적용

- Ollama 모델 조회 시 m.name 외에 m.model 필드까지 fallback 파싱 처리해 호환성 극대화 (routes/settings.ts, ConnectionsPanel.tsx)
- 엔진 전환 또는 핑 성공 시, 기존 모델명이 목록에 존재하지 않을 때 드롭다운에 빈칸이 렌더링되는 문제를 예방하기 위한 자동 정합성 교정 useEffect 도입 (ConnectionsPanel.tsx)
- 타입 검사(tsc) 및 Vite 프로덕션 빌드 무결성 확인 및 빌드 테스트 완료
